# Domain Docs

Before exploring, read root `CONTEXT.md` when it exists and ADRs under
`docs/adr/` that affect the current area. Proceed silently when they do not
exist.

This repository uses the single-context layout: root `CONTEXT.md` and
repository-wide ADRs under `docs/adr/`. Use glossary terms consistently and
surface conflicts with existing ADRs.
